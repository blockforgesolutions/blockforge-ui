import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["res.cloudinary.com","templates.simplified.co","iakademi.com","storage.googleapis.com"],
  },
};

export default nextConfig;
